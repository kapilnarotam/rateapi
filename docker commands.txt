docker pull mysql/mysql-server:5.7
docker run -d -P --name='rateapidb' -e MYSQL_ALLOW_EMPTYPASSWORD=true mysql
docker build -t demo .
docker run -p 3000:3000 demo

need to update bundler on main rateapi